// @chatwing
{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // Add libs for DOM.
    "lib": ["ESNext", "DOM", "DOM.Iterable"],
    // Output .js files in the lib folder, which won't be used, but we can't
    // disable emit in this composite project.
    "outDir": "./lib"
  },
  "include": ["../../types.d.ts", "**/*.ts", "**/*.tsx", "**/*.json"]
}
